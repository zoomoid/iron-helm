apiVersion: v1
kind: Secret
metadata:
  name: {{ include "minecraft.fullname" . }}-secrets
  labels:
    {{- include "minecraft.labels" . | nindent 4 }}
stringData:
  MCRCON_PASS: {{ include "minecraft.rconPassword" .Values.server.config }}
